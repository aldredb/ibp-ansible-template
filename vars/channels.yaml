consortium_members:
  - org1
  - org2

channels:
  samplechannel1:
    members:
      # has to match the key in peer_organizations
      - org1
      - org2
    operators:
      - org1
      - org2
    config_update_policy: 2 # out of total number of operators
    chaincodes:
      marbles:
        version: v1
        constructor:
          function: "init"
          args:
            - "test"
          endorsement_policy: OR('org1msp.peer','org2msp.peer')
  samplechannel2:
    members:
      - org1
    operators:
      - org1
    config_update_policy: 1
